
int CONFIG_SNAPCAST_CLIENT_NAME(int i2s_num, const char *src, size_t size, int delay);
int CONFIG_SNAPCAST_CLIENT_NAME_expand(int i2s_num, const char *src, size_t size, int from, int to, int delay);
